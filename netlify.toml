[build]
  command   = "npm run build"
  publish   = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# App Store Lookup makes multiple external HTTP requests — needs extra time
[functions]
  node_bundler = "nft"
  timeout      = 26
